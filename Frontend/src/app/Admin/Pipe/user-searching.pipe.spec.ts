import { UserSearchingPipe } from './user-searching.pipe';

describe('UserSearchingPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSearchingPipe();
    expect(pipe).toBeTruthy();
  });
});
